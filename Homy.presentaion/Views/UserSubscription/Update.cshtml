@model Homy.Application.Dtos.UserDtos.UserSubDTOs.UserSubUpdateDTO

@{
    ViewData["Title"] = "تعديل الاشتراك";
}

<h1>تعديل الاشتراك</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Update" asp-route-userId='@ViewData["UserId"]'>
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            <div class="form-group mb-3">
                <label asp-for="EndDate" class="control-label">تاريخ الانتهاء</label>
                <input asp-for="EndDate" class="form-control" type="date" />
                <span asp-validation-for="EndDate" class="text-danger"></span>
            </div>
            
            <div class="form-group mb-3 form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="IsActive" /> نشط
                </label>
            </div>
            
            <div class="form-group">
                <input type="submit" value="حفظ" class="btn btn-primary" />
                <a asp-action="GetAll" class="btn btn-secondary">العودة للقائمة</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
